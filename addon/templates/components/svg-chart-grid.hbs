{{#each horzLines as |line|}}
    <line x1="{{line.x1}}" y1="{{line.y1}}" x2="{{line.x2}}" y2="{{line.y2}}" shape-rendering="crispEdges" />
{{/each}}

{{#each vertLines as |line|}}
    <line x1="{{line.x1}}" y1="{{line.y1}}" x2="{{line.x2}}" y2="{{line.y2}}" shape-rendering="crispEdges" />
{{/each}}
